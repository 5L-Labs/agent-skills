# Scripts for YouTube Batch Processor

## generate_luna_digest.py

Generates a Luna-style digest from a timestamped transcript file.

Usage:
```bash
python3 generate_luna_digest.py timestamped_transcript.txt
```

The script reads the corresponding `.json` file (if available) for video metadata.
